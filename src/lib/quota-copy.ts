/** Trial quota — single source for UI + metadata (keep in sync). */
export const QUOTA_TRIAL_LABEL = "體驗說明";
export const QUOTA_TRIAL_BODY =
  "每個帳戶可免費分析一次；全站每日最多 10 個名額，先到先得。";
export const QUOTA_TRIAL_ARIA_LABEL = `${QUOTA_TRIAL_LABEL}。${QUOTA_TRIAL_BODY}`;
